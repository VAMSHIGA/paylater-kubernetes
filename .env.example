# PayLater — Docker Compose environment template
# Copy to .env and replace placeholder values before running:
#   cp .env.example .env

# MySQL root password (required)
# Used by: mysql container, healthcheck, and DB_PASSWORD for all microservices
MYSQL_ROOT_PASSWORD=your-mysql-root-password

# JWT signing secret (required)
# Use a long random string; must match across all six microservices
JWT_SECRET=your-jwt-secret-key

# MySQL application user for microservices (optional, default: root)
DB_USER=root

# Docker image tag for application services (optional, default: latest)
# For production, set APP_VERSION to a specific git SHA or release tag.
APP_VERSION=latest
# Use 3308 when port 3306 is already in use on your machine.
# Microservices always connect to mysql:3306 inside Docker — do not change DB_PORT.
MYSQL_PORT=3308
