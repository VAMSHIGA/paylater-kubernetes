# Identity Service — environment variables for local development.
# Copy to .env for local development. Do not commit secrets.

HTTP_PORT=8081

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change_me
DB_NAME=identity_db

JWT_SECRET=change_me_to_strong_random_secret
