# Transaction Service — environment variables for local development.

HTTP_PORT=8084

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change_me
DB_NAME=transaction_db

# Optional cross-database names for credit-limit checks (defaults shown).
# CUSTOMER_DB=customer_db
# TRANSACTION_DB=transaction_db
# PAYBACK_DB=payback_db

JWT_SECRET=change_me_to_strong_random_secret
