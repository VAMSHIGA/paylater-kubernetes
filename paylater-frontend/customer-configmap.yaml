apiVersion: v1
kind: ConfigMap

metadata:
  name: customer-config
  namespace: paylater-dev

data:
  HTTP_PORT: "8082"
  DB_HOST: "mysql-service"
  DB_PORT: "3306"
  DB_NAME: "customer_db"
  DEFAULT_CUSTOMER_CREDIT_LIMIT: "1000.00"
